# features

Feature modules (feature-driven design). Each feature owns its components, hooks, services, types, and tests together. Example: `features/journal/`, `features/auth/`. Cross-feature sharing only via `components/` (design system) and `lib/`.
