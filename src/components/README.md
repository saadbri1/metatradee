# components

Shared, presentational design-system components (dumb, styled with tokens, accessible by construction). `ui/` holds shadcn/ui primitives. Components here NEVER fetch data. Smart, data-aware components live in `features/`.
