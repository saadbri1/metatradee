# contexts

React context providers for cross-cutting, non-server state that must live in the tree. Prefer Zustand (`store/`) for global ephemeral UI state; use context only when the tree genuinely needs it.
